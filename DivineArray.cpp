#include <iostream>
#include <vector>

using namespace std;
int main()
{
    int t, n, inp, q, aX, k;
    vector<int> temp;
    vector<int> arr;
    vector<int> occur;
         cin >> t;
    while (t--)
    {
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            cin >> inp;
            arr.push_back(k);
        }
        cin >> q;
        //vector<int> query(n, INT_MAX);
        for (int j = 0; j < q; j++)
        {
            cin >> aX >> k;
            // query.at(aX) = k; // aX stores index of value we want in arr & k = kth step
            temp=arr;
            for (int i = 0; i < k; i++)
            {
                
            }
            

        }


        
    }

    return 0;
}